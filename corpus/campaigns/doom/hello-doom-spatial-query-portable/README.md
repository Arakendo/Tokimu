# Portable Doom spatial-query fixture

This tiny consumer adapts four exact E1M1 triangles retained by the camera
query study into generic finite members. It executes the same corpus-local
spatial-query implementation under native Rust and `wasm32-unknown-unknown`.

Doom identities and coordinate selection remain here. The shared study crate
does not know their source vocabulary and owns no visibility or presentation
authority.
