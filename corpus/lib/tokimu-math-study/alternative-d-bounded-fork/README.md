# Alternative D: Bounded Fork/Copy

This area tests a deliberately bounded subset derived from the exact audited
upstream source. Every derived file or section must retain reviewable
provenance, licensing, attribution, and local modification history.

The first scalar `Vec3` slice is in `src/alternative_d.rs`. Its exact revision,
source, licensing, and local changes are retained in `UPSTREAM-NOTICE.md`.
